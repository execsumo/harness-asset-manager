import Foundation

/// SwiftConfigDocument: Source-preserving configuration document engine spike in Swift.
/// Evaluates TOML, YAML, JSONC, and JSON round-trip source preservation against the Python baseline.
public struct SwiftConfigDocumentModule {
    public static let version = "0.1.0"
}
